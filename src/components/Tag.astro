---
interface Props {
  label: string;
}
const { label } = Astro.props;
---
<span class="font-mono text-[0.6875rem] tracking-[0.02em] px-2 py-0.5 border border-border-subtle text-text-muted">{label}</span>
